<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light d-flex flex-lg-column">
        <div class="container-fluid">
            <a href="/" class="navbar-brand m-auto">
                <img src="/images/logo.png" alt="logo del blog" class="m-auto" width="100" height="100">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse text-center text-dark" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item my-1">
                        <a href="/admin/index" class="navbar-brand"><span class="container">
                            <% if(usuario.avatar && usuario.avatar != "") { %>
                                <img src="/avatars/<%=usuario.avatar%>" alt="avatar del usuario" class="avatar">
                            <% } else { %>
                                <i class="fa fa-user fa-fw"></i>
                            <% } %>
                        </span><%=usuario.pseudonimo%></a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/logout" class="btn btn-warning"> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>